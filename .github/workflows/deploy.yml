name: 🚀 CI/CD — Olivier Trevis

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  build:
    name: 🏗️ Build
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: 📚 Install dependencies
        run: npm ci

      - name: 🏗️ Build
        run: npm run build
        env:
          NODE_ENV: production

      - name: ✅ Build OK
        run: echo "✨ Olivier Trevis — build réussi"

  deploy:
    name: 🚂 Deploy to Railway
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == "refs/heads/main" || github.ref == "refs/heads/master"
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4

      - name: 🚂 Deploy to Railway
        uses: bervProject/railway-deploy@main
        with:
          railway_token: ${{ secrets.RAILWAY_TOKEN }}
          service: oliviertrevis-site

      - name: ✅ Deployed
        run: echo "🚀 Olivier Trevis déployé sur Railway — Js-Innov.IA"
